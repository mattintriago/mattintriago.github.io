import styled from "styled-components";

const Home = (props) => {
  return (
    <Container>
      <Wrap>
      </Wrap>
    </Container>
  );
};

const Container = styled.div`
  margin-top: 100px;
`;

const Wrap = styled.div``;

export default Home;